# Trading View

## How to start

1. Install dependencies via npm install

2. Copy charting_library folder to /public.

3. Copy charting_library folder to /src.

4. Copy datafeeds folder to /public.

5. Run npm start. It will build the project and open a default browser with the Charting Library.
